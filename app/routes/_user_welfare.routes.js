module.exports = function (app) {
    var controller = require('../controllers/user_welfare.controller');
    // app.get('/', controller.list);
    // app.get('/id/:id', controller.listId); //welfare.route
    // app.get('/groupYear', controller.groupYear); //group/welfare.router
    // app.get('/groupByYear/year/:year', controller.groupByYear); //group/welfare.route
    // app.get('/adminEmployee/:welId', controller.adminEmployee); //group/welfare.route
    // app.get('/welfaresEmployee/:id', controller.welfaresEmployee); //employee.route
}